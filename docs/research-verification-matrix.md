# Research Verification Matrix

This matrix maps the current repository evidence to the remaining roadmap gaps in `research/pipeline_builder_cpp_research_plan.md`. It is release-facing evidence control, not an implementation plan by itself.

## Scope and source anchors

Source-of-truth research anchors with line ranges from `research/pipeline_builder_cpp_research_plan.md`:

- Lines 989-997, Phase 5 branch/join requirements: type-level branch node, predicate requirements, branch output validation, join validation, sequential branch execution, and branch diagnostics.
- Lines 998-1005, Phase 6 optional backend requirements: thread-pool executor plus oneTBB, Taskflow, stdexec prototypes, and a backend feature matrix.
- Lines 1006-1013, Phase 7 packaging/release requirements: install targets, package config files, examples, docs, CI compiler matrix, and v0.1.0 publication.
- Lines 1324-1336, acceptance criteria: sequential valid chains, compile-time invalid edge diagnostics, expected-like failure propagation, adapters, throwing-function runtime errors, GCC/Clang public-header coverage, compile-time benchmark records, and one success plus one failure example.
- Lines 1490-1501 and 1518, compile-time optimization notes: compile-time baselines/regression tracking and release-quality benchmark gates remain explicit goals.

## Gap-to-evidence matrix

| Research area | Current implemented evidence | Current test/evidence surface | Release status | Next required slice |
| --- | --- | --- | --- | --- |
| Linear MVP core | Typed linear `pb::from<T>::then<S>::to<U>` DSL, stage traits, describe/metadata helpers, validation diagnostics, and public aliases. | Compile-pass/compile-fail suites, public-header smoke tests, examples, and `docs/roadmap-gap-map.md`. | Shipped MVP support. | Keep diagnostics wording hardened and refresh compile-fail evidence on each release candidate. |
| User adapters | Free-function, function-object, functor/member adapter surfaces with recent adapter/member hardening in `include/pb/adapt/fn.hpp`. | Adapter compile-pass/compile-fail tests and runtime adapter smoke tests. | Shipped MVP support for current adapter shapes. | Keep adapter docs limited to supported callable shapes; add examples only when they match tested behavior. |
| Runtime result/error model | Sequential runtime, `run()` / `try_run()`, expected-like/result normalization, custom error conversion, observer callbacks, exception capture boundaries, and a narrow sequential stage-storage policy namespace. | Runtime smoke tests, result tests, observer smoke tests, policy/stateful storage smoke tests, `docs/error-model-tutorial.md`, `docs/examples.md`, and `docs/observer-hooks-roadmap.md`. | Shipped sequential MVP support with policy gaps called out. | Harden broader error/exception policy wording and avoid claiming stable observer ABI/event schema. |
| Branch/join topology | Public `::branch<CaseA, CaseB>::to<Output>` and `::branch<CaseA, CaseB>::join<JoinStage>::to<Output>` DSL with compile-time validation, runtime sequential branch execution (first-match-wins, short-circuit), observer events (`on_case_selected`, `on_case_skipped`), stateful storage, join stages, error propagation, raw and unified output validation helpers, first-slice heterogeneous outputs through `std::variant` plus type-list selected-output joins including duplicate output alternatives by index, const-input predicate evaluation, move-only selected-branch input consumption, and comprehensive tests and examples. | Compile-pass tests including `branch_join_validation_compile_pass.cpp` and `branch_heterogeneous_compile_pass.cpp`, compile-fail diagnostics for join-without-branch, join-type-mismatch, and unified-output validation mismatch, runtime branch tests including heterogeneous, stateful, and move-only coverage, branch examples, `docs/branch-join-roadmap.md`. | Supported for homogeneous outputs, variant-based heterogeneous outputs including duplicate output alternatives by index, type-list selected-output joins, and move-only selected-branch input consumption. | Keep parallel all-branches fan-in / true backend multi-input joins, move-only predicates that consume by value, and backend branch execution as separate design/implementation phases. |
| Graph export | DOT/JSON helper evidence covers linear and supported branch pipelines, including branch topology detection in JSON, DOT label escaping, and helper-output golden regressions. Descriptor-record-backed helper rendering exists for supported branch DOT/JSON output, while stable descriptor/export schemas, CLI/file export, and stable runtime descriptor/export contracts remain unshipped. | `pb_export_dot_compile_pass`, `pb_export_dot_branch_compile_pass`, `pb_export_json_compile_pass`, `pb_export_json_branch_compile_pass`, `pb_export_golden_compile_pass`, and `docs/graph-export-roadmap.md`. | Partial descriptor-record-backed helper support; stable descriptor/export compatibility remains roadmap-only. | Define stable descriptor/export schema after branch/join and descriptor semantics are settled, then promote helper-output golden regressions into release-grade JSON/DOT/descriptor compatibility fixtures and examples before broader claims. |
| Stable runtime descriptor | Current describe/metadata helpers support linear introspection, descriptor alias symmetry coverage, runtime `error_record` / `to_record(...)` diagnostic projection, and tests that align descriptor stage identity with observer failure/exception callbacks, custom expected-like diagnostic annotation, and the `try_run()` error envelope, but no stable runtime descriptor/export contract is promised. | `include/pb/core/describe.hpp`, `include/pb/runtime/error.hpp`, descriptor compile-pass tests, runtime diagnostic smoke tests, linear descriptor/observer identity smoke tests, `docs/runtime-descriptor-roadmap.md`. | Partial MVP introspection only; stable descriptor remains roadmap. | Define ownership, value schema, versioning, and export compatibility before presenting descriptor output as a stable public contract. |
| Optional backends | No thread-pool, oneTBB, Taskflow, or stdexec executor is supported today. A standalone standard-library `thread_pool` utility exists, while the public backend feature matrix marks only `sequential` as supported/default-build and CMake fails fast if reserved external backend flags are enabled. | `include/pb/runtime/backend.hpp`, `include/pb/runtime/thread_pool.hpp`, `pb_runtime_thread_pool_smoke`, `pb_runtime_backend_feature_matrix_smoke`, fail-fast configure checks for `PB_BACKEND_TASKFLOW`, `PB_BACKEND_TBB`, and `PB_BACKEND_STDEXEC`, and `docs/optional-backends-roadmap.md`. | Utility plus matrix/gating done; non-sequential pipeline execution remains roadmap-only. | Prototype one backend without leaking backend APIs into core headers, then add adapter tests, examples, dependency isolation evidence, and benchmark data before any support claim. |
| Benchmark thresholds and compile-time budgets | Benchmark smoke targets and Clang time-trace workflow exist, but no release threshold or regression budget is enforced. | `bench/*`, `docs/benchmark-workflow.md`, `docs/benchmark-evidence-template.md`, CTest benchmark labels. | Smoke/profiling scaffolding only. | Record baseline numbers for 5-stage and 50-stage chains, then add CI/regression budget policy before treating performance as a release gate. |
| Release governance | CMake install/package config smoke scaffolding, release checklist, and cross-compiler validation workflow exist. Latest code-validation evidence passed for GCC C++20/C++23, Clang C++20/C++23, MSVC C++20, and clean Ubuntu package-release on SHA `6805543ede6946aa283be7f24fb3736c762f47b2`. | `docs/release-readiness-checklist.md`, `docs/release-evidence-template.md`, `docs/cross-compiler-validation.md`, `.github/workflows/cross-compiler-validation.yml`, package presets, `pb_package_config_smoke`. | Release scaffolding plus current matrix evidence shipped; exact final tag-SHA evidence is still required if non-doc code changes land. | Rerun package-release and cross-compiler workflows on the final candidate SHA, record compiler/preset/archive/run evidence, then tag. |

## Active priority backlog for release coordination

The docs lane tracks these priorities so release notes and PR text do not overclaim in-progress work from other workers:

| Priority | Research-plan anchor | Status to claim today | Required evidence before promotion |
| --- | --- | --- | --- |
| 1-2 | Phase 2 runtime result/error and expected-like behavior | Sequential MVP support with explicit policy gaps. | Runtime tests and docs that prove the final `run()` / `try_run()` / `std::expected` behavior on supported compilers. |
| 3 | Compile-time optimization notes, lines 1490-1501 and 1518 | Benchmark smoke scaffolding only. | Baseline records for 5-stage and 50-stage chains plus context. |
| 4 | Runtime stage ownership/lifetime assumptions | Narrow sequential stateful-storage guarantee exists for default-initializable stages, including non-copyable owned stage state. Borrowed/shared/unique ownership, reset behavior, and thread-local future-backend storage remain missing. | Policy aliases, `pb_runtime_sequential_policy_dsl_smoke`, `pb_runtime_stateful_storage_policy_smoke`, and `docs/examples.md`. |
| 5-6 | Phase 5 branch output and join validation | Accepted validation evidence exists for branch output compatibility and join consumption; keep it scoped to validation, not execution. | Final candidate branch includes the validation commits plus branch-output and join-validation compile-pass/compile-fail logs. |
| 7 | Phase 5 sequential branch execution | Supported for the current sequential slice: homogeneous outputs, variant-based heterogeneous outputs plus type-list selected-output joins including duplicate alternatives by index, move-only selected-branch input consumption, observer events, stateful storage, tests, and examples. | Parallel all-branches fan-in / true backend multi-input joins, backend branch execution, and broader move-only predicate policies need design, tests, and docs before promotion. |
| 8-9 | Descriptor/export and graph export themes | Compile-time metadata exists and DOT/JSON helpers cover linear plus supported branch pipelines with DOT label escaping and helper-output golden regressions; stable descriptor/export compatibility remains roadmap-only. | Stable schema/export API, descriptor-backed golden compatibility tests, CLI/file behavior, and docs; distinguish helper output from full graph export. |
| 10 | Phase 6 optional backends | Sequential backend only; backend feature matrix and fail-fast external backend gates are documented/tested. | Implementation, isolation, tests, examples, and benchmark evidence before any new backend support claim. |
| 11-12 | Phase 7 compiler/release requirements | Current matrix evidence exists for SHA `6805543`: GCC C++20/C++23, Clang C++20/C++23, MSVC C++20, and clean Ubuntu package-release all passed. | Keep exact-SHA compiler ID/version plus configure/build/test/package logs attached before tagging; rerun if later non-doc code changes land. |

## Runtime lane gap/evidence refresh

| Area | Moved from | Current status | Evidence | Still missing |
| --- | --- | --- | --- | --- |
| Optional backends | Missing / placeholder CMake flags only | Partial: feature matrix plus fail-fast reserved external backend flags; no new backend execution support | `include/pb/runtime/backend.hpp`, `pb_runtime_backend_feature_matrix_smoke`, fail-fast configure checks for Taskflow/oneTBB/stdexec flags | Thread-pool, Taskflow, oneTBB, stdexec adapters; examples; dependency isolation target tests; backend benchmarks |
| Policy DSL | Missing full DSL | Partial: `pb::policy` / `pb::runtime::policy` exposes the existing sequential stage-storage policy seam | `pb_runtime_sequential_policy_dsl_smoke`, public-header static assertions, `docs/examples.md` | `::with<pb::policy::...>`, error/exception/reference-lifetime/contract/diagnostic verbosity/executor capability policies |
| Stateful stage storage | Partial / not a public guarantee | Narrow done: default-initializable sequential stages may use per-run construction or engine-stored state, including non-copyable owned members, across `run()` and `try_run()` | `pb_runtime_stateful_storage_policy_smoke`, `pb_runtime_sequential_policy_dsl_smoke`, `docs/examples.md`, `docs/roadmap-gap-map.md` | Borrowed/reference, shared/unique ownership, reset, move-in stage instances, and thread-local policies for future parallel backends |
| Standard-library runtime boundary | At risk from optional-backend placeholders | Done for this slice: runtime remains standard-library-only; external backend flags fail fast | Dependency grep, fail-fast configure checks for all reserved external backend flags, backend matrix marks only sequential supported | Future backend targets must keep external dependencies isolated from core/runtime public headers |

## Current release decision rule

A release note may claim the linear/sequential MVP, current adapter shapes, smoke benchmark scaffolding, package-consumer scaffolding, supported sequential branch execution, variant-based heterogeneous branch outputs including duplicate alternatives by index, move-only selected-branch input consumption, DOT/JSON helper export for linear plus supported branch pipelines, and the currently exercised GCC/Clang/MSVC/package validation matrix only when fresh candidate evidence is attached. It must keep parallel all-branches fan-in / true backend multi-input joins, stable descriptor/export compatibility, CLI/file export, optional backend pipeline execution, benchmark thresholds, stable observer ABI/event schema, and CI-enforced performance budgets marked as roadmap gaps.

## Evidence refresh checklist

Before changing any row from roadmap/partial to shipped support, require all three:

1. Code/API evidence in the owned surface.
2. Targeted compile-pass, compile-fail, runtime, benchmark, or package tests proving the behavior.
3. Documentation/examples that state the boundary without promoting future roadmap behavior.
