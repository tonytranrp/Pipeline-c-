# Research Verification Matrix

This matrix maps the current repository evidence to the remaining roadmap gaps in `research/pipeline_builder_cpp_research_plan.md`. It is release-facing evidence control, not an implementation plan by itself.

## Scope and source anchors

Source-of-truth research anchors with line ranges from `research/pipeline_builder_cpp_research_plan.md`:

- Lines 989-997, Phase 5 branch/join requirements: type-level branch node, predicate requirements, branch output validation, join validation, sequential branch execution, and branch diagnostics.
- Lines 998-1005, Phase 6 optional backend requirements: thread-pool executor plus oneTBB, Taskflow, stdexec prototypes, and a backend feature matrix.
- Lines 1006-1013, Phase 7 packaging/release requirements: install targets, package config files, examples, docs, CI compiler matrix, and v0.1.0 publication.
- Lines 1324-1336, acceptance criteria: sequential valid chains, compile-time invalid edge diagnostics, expected-like failure propagation, adapters, throwing-function runtime errors, GCC/Clang public-header coverage, compile-time benchmark records, and one success plus one failure example.
- Lines 1490-1501 and 1518, compile-time optimization notes: compile-time baselines/regression tracking and release-quality benchmark gates remain explicit goals.

## Gap-to-evidence matrix

| Research area | Current implemented evidence | Current test/evidence surface | Release status | Next required slice |
| --- | --- | --- | --- | --- |
| Linear MVP core | Typed linear `pb::from<T>::then<S>::to<U>` DSL, stage traits, describe/metadata helpers, validation diagnostics, and public aliases. | Compile-pass/compile-fail suites, public-header smoke tests, examples, and `docs/roadmap-gap-map.md`. | Shipped MVP support. | Keep diagnostics wording hardened and refresh compile-fail evidence on each release candidate. |
| User adapters | Free-function, function-object, functor/member adapter surfaces with recent adapter/member hardening in `include/pb/adapt/fn.hpp`. | Adapter compile-pass/compile-fail tests and runtime adapter smoke tests. | Shipped MVP support for current adapter shapes. | Keep adapter docs limited to supported callable shapes; add examples only when they match tested behavior. |
| Runtime result/error model | Sequential runtime, `run()` / `try_run()`, expected-like/result normalization, custom error conversion, observer callbacks, exception capture boundaries, a narrow sequential stage-storage policy namespace, **and a parity-hardened five-wrapper engine-side error-policy DSL** (`pb::with_throw_on_error` → `throwing_engine`, `pb::with_terminate_on_error` → `terminating_engine`, `pb::with_ignore_errors` → `ignoring_engine`, `pb::with_propagate_exceptions` → `propagating_engine`, `pb::with_verbose_diagnostics` → `verbose_engine` owning a `pb::verbose_observer`). Each wrapper exposes `set_observer` / `get_observer` / `describe` / `descriptor` directly, so `verbose_engine` composes over any of the other four wrappers. `std::expected`-shape stages flow through every wrapper with `error_category::expected_error` normalization. | Runtime smoke tests, result tests, observer smoke tests, policy/stateful storage smoke tests, `tests/runtime/throw_on_error_smoke.cpp`, `tests/runtime/policy_dsl_engine_smoke.cpp`, `tests/compile_pass/terminate_on_error_smoke.cpp`, `tests/runtime/policy_dsl_parity_matrix_linear.cpp`, `tests/runtime/policy_dsl_parity_matrix_branch.cpp`, `tests/runtime/policy_dsl_parity_matrix_fan_in.cpp`, `tests/runtime/policy_dsl_composability_smoke.cpp`, `tests/runtime/policy_dsl_std_expected_matrix.cpp`, `include/pb/runtime/error_policy.hpp`, `docs/error-model-tutorial.md`, `docs/examples.md`, and `docs/observer-hooks-roadmap.md`. | Sequential MVP + wrapper-level error-policy DSL shipped with cross-wrapper `run()` / `try_run()` parity proven over linear, selected-output branch, and explicit fan-in topologies and over `std::expected`-shape stages. | Runtime-enforced `::with<pb::policy::...>` DSL beyond marker bundles, unified policy-surface narrative docs, and stable observer ABI/event schema remain roadmap-only. |
| Branch/join topology | Public `::branch<CaseA, CaseB>::to<Output>`, selected-output `::join<JoinStage>`, and fan-in `::fan_in<JoinStage>` / `::join_all<JoinStage>` DSLs with compile-time validation, runtime sequential selected-output branch execution (first-match-wins, short-circuit), runtime sequential fan-in execution (all predicates, all passing cases in declaration order, skipped/completed/failed slots, void-output cases, and borrowed move-only inputs), thread-pool backend fan-in scheduling with deterministic aggregate ordering, observer events (`on_case_selected`, `on_case_skipped`, `on_case_failed`), stateful storage, join stages, error propagation, raw and unified output validation helpers, heterogeneous outputs through `std::variant` plus type-list selected-output joins including duplicate output alternatives by index, const-input predicate evaluation, move-only selected-branch input consumption, **`pb::shared_view<T>` shared-ptr-backed fan-in input wrapper for non-copyable owned values, `pb::projected<From, Projection, Stage>` projection-adapter for plug-in extract/borrow strategies, `pb::fan_in_uses_copy_v` / `pb::fan_in_uses_borrow_v` compile-time introspection**, and comprehensive tests and examples. | Compile-pass tests including `branch_join_validation_compile_pass.cpp`, `branch_heterogeneous_compile_pass.cpp`, and `branch_fan_in_compile_pass.cpp`; compile-fail diagnostics for join-without-branch, join-type-mismatch, unified-output validation mismatch, and invalid fan-in joins; runtime branch tests including heterogeneous, stateful, move-only, fan-in, and thread-pool fan-in coverage; `tests/runtime/fan_in_shared_view_smoke.cpp`, `tests/runtime/projected_stage_smoke.cpp`; `include/pb/runtime/clone.hpp`; branch examples; `docs/branch-join-roadmap.md`. | Supported for homogeneous outputs, variant-based heterogeneous outputs including duplicate output alternatives by index, type-list selected-output joins, move-only selected-branch input consumption, sequential fan-in aggregates with skipped/completed/failed states, diagnostics, void-output slots, borrowed move-only inputs, thread-pool fan-in scheduling, and `pb::shared_view` / `pb::projected` clone-projection policies. | Keep preemptive cancellation, dependency backend fan-in, owned per-case unique-clone policy beyond shared-view, move-only predicates that consume by value, and broad backend branch execution as separate design/implementation phases. |
| Graph export | DOT/JSON/text helper evidence covers linear, selected-output branch, and explicit fan-in pipelines, including the `pb.core.graph.v1` typed schema-version contract, deterministic branch case identity fields, DOT label escaping, JSON string escaping, helper-output golden regressions, byte-equal extended goldens, **the `pb::export_text` compact text helper**, and a substantially expanded `pb_cli` introspection CLI exposing `list` / `describe --format=<dot\|json\|text>` / `features`. Descriptor-record-backed helper rendering exists for supported branch DOT/JSON output, while stable external descriptor/export schemas, CLI/file export for arbitrary user pipelines, backend scheduling/trace export, and frozen external schema semantics remain unshipped. | `pb_export_dot_compile_pass`, `pb_export_dot_branch_compile_pass`, `pb_export_json_compile_pass`, `pb_export_json_branch_compile_pass`, `pb_export_golden_compile_pass`, `tests/compile_pass/export_golden.cpp`, `tests/compile_pass/schema_v1_contract.cpp`, `tests/compile_pass/schema_v1_extended_golden.cpp`, `tests/compile_pass/export_text.cpp`, `tests/compile_pass/export_text_golden.cpp`, `tests/compile_pass/public_headers/export_text.cpp`, `tests/run_pb_cli_describe.cmake`, `include/pb/core/export_text.hpp`, `tools/pb_cli.cpp`, `docs/export-schema-v1.md`, `docs/export-helper-schema.md`, and `docs/graph-export-roadmap.md`. | Production-grade helper surface for supported shapes; the `pb.core.graph.v1` typed contract pins schema-version string and field layout via byte-equal goldens, but a frozen external interchange schema with stability promise and migration policy remains roadmap-only. | Promote helper-output golden regressions into release-grade compatibility fixtures with a v1 stability promise (no field removal without major bump) and a documented v1 → v2 migration path; expand the `pb_cli` surface to accept user-defined pipelines once that migration policy is decided. |
| Stable runtime descriptor | Current describe/metadata helpers support linear, selected-output branch, and explicit fan-in introspection, descriptor alias symmetry coverage, runtime `error_record` / `to_record(...)` diagnostic projection, indexed I/O aliases (`pipeline_stage_input_t<P, N>` / `pipeline_stage_output_t<P, N>`) with named out-of-range diagnostics, and tests that align descriptor stage identity with observer failure/exception callbacks, custom expected-like diagnostic annotation, and the `try_run()` error envelope. The `pb.core.graph.v1` typed schema-version contract anchors the descriptor/export surface to a versioned identifier, but no stable runtime descriptor/export *external* contract is promised. | `include/pb/core/describe.hpp`, `include/pb/core/pipeline.hpp` (indexed I/O alias re-exports), `include/pb/runtime/error.hpp`, descriptor compile-pass tests, `tests/compile_pass/pipeline_stage_io_aliases.cpp`, `tests/compile_fail/pipeline_stage_input_index_oob.cpp`, `tests/compile_fail/pipeline_stage_output_index_oob.cpp`, `tests/compile_pass/schema_v1_contract.cpp`, runtime diagnostic smoke tests, linear descriptor/observer identity smoke tests, `docs/runtime-descriptor-roadmap.md`, `docs/export-schema-v1.md`. | Partial MVP introspection plus typed `pb.core.graph.v1` schema-version contract; a frozen external descriptor remains roadmap. | Define ownership, value schema, versioning policy, schema-migration rules (v1 → v2), and export compatibility before presenting descriptor output as a stable public contract. |
| Optional backends | `thread_pool_backend` is supported for the current standard-library fan-in backend slice. It schedules passing fan-in cases concurrently, preserves declaration-order aggregate results, drains running cases, and keeps external backend flags fail-fast. oneTBB, Taskflow, and stdexec remain roadmap/experimental. | `include/pb/runtime/backend.hpp`, `include/pb/runtime/thread_pool.hpp`, `include/pb/runtime/thread_pool_backend.hpp`, `pb_runtime_thread_pool_smoke`, `pb_runtime_thread_pool_fan_in_smoke`, `pb_runtime_backend_feature_matrix_smoke`, fail-fast configure checks for `PB_BACKEND_TASKFLOW`, `PB_BACKEND_TBB`, and `PB_BACKEND_STDEXEC`, and `docs/optional-backends-roadmap.md`. | Supported for the fan-in thread-pool slice; dependency backends and broader parallel semantics remain roadmap-only. | Add backend examples/benchmarks, broader cancellation/error policies, and dependency backend adapters only with isolation tests and matrix evidence. |
| C++26 feature gates and reflection adapter | Typed `pb::features::*` `inline constexpr bool` gate constants ship for `has_cpp26`, `has_reflection`, `has_contracts`, `has_pack_indexing`, `has_std_expected`, and `has_deducing_this`, alongside their `PB_HAS_*` macro counterparts with user-override support and implication invariants. `pb::reflect_stage<T>` ships as a gated forward-compat scaffold in `include/pb/adapt/reflect.hpp`: when `PB_HAS_REFLECTION == 1` it emits a working reflection-driven adapter, and on C++20 baselines instantiating it produces a clear `static_assert` pointing at the gate. `pb::reflect_stage_available_v<T>` mirrors the macro. | `include/pb/core/cpp26_features.hpp`, `include/pb/adapt/reflect.hpp`, `tests/compile_pass/cpp26_features_fallback.cpp`, `tests/compile_pass/reflect_stage_gate.cpp`, `docs/cpp26-feature-gates.md`. | Typed gate constants + fallback-tested forward-compat seam shipped; C++26 reflection is **not** supported on current toolchains. | Add a working reflection-driven adapter once a C++26 reflection toolchain baseline is established, plus broader compiler/library detection tests and release-grade C++26 feature-gate validation in CI. |
| Benchmark thresholds and compile-time budgets | Compile-time/header smoke targets are available in test-enabled developer presets, with a `pb_compile_time_benchmarks` aggregate target covering header inclusion plus 5-stage and 50-stage chains. Clang time-trace workflow exists, but no release threshold or regression budget is enforced. | `bench/compile_time/*`, `bench/CMakeLists.txt`, `docs/benchmark-workflow.md`, `docs/benchmark-evidence-template.md`, CTest labels `benchmark`, `compile_time`, `header`, and `bench`. | Reproducible smoke baseline scaffolding exists; measured budgets are not enforced. | Record release-candidate timing numbers and add CI/regression budget policy before treating performance as a release gate. |
| Release governance | CMake install/package config smoke scaffolding, release checklist, and cross-compiler validation workflow exist. Latest GitHub code-validation evidence passed for GCC C++20/C++23, Clang C++20/C++23, MSVC C++20, and clean Ubuntu package-release on SHA `87299c14c813753d170911239e251064cbbfee6f`; the same SHA also passed local Clang developer and package-release verification, plus normal CI Clang dev/package/benchmark lanes. | `docs/release-readiness-checklist.md`, `docs/release-evidence-template.md`, `docs/cross-compiler-validation.md`, `.github/workflows/cross-compiler-validation.yml`, package presets, `pb_package_config_smoke`. | Release scaffolding plus exact code-SHA local and GitHub matrix evidence are current for this candidate. | Record compiler/preset/archive/run evidence in release notes, rerun if later non-doc code changes land, then tag. |

## Active priority backlog for release coordination

The docs lane tracks these priorities so release notes and PR text do not overclaim in-progress work from other workers:

| Priority | Research-plan anchor | Status to claim today | Required evidence before promotion |
| --- | --- | --- | --- |
| 1-2 | Phase 2 runtime result/error and expected-like behavior | Sequential MVP plus parity-hardened five-wrapper error-policy DSL (`throwing` / `terminating` / `ignoring` / `propagating` / `verbose` engines). Cross-wrapper `run()` / `try_run()` parity is proven over linear, selected-output branch, and explicit fan-in topologies; wrappers compose (verbose around any of the other four) via the directly-exposed `set_observer` / `get_observer` / `describe` / `descriptor` surface; `std::expected`-shape stages flow through every wrapper with `error_category::expected_error` normalization. | Runtime-enforced `::with<pb::policy::...>` DSL beyond marker bundles, full policy-surface narrative docs, and stable observer ABI/event schema. |
| 3 | Compile-time optimization notes, lines 1490-1501 and 1518 | Benchmark smoke scaffolding only. | Baseline records for 5-stage and 50-stage chains plus context. |
| 4 | Runtime stage ownership/lifetime assumptions | Narrow sequential stateful-storage guarantee exists for default-initializable stages, including non-copyable owned stage state. Borrowed/shared/unique ownership, reset behavior, and thread-local future-backend storage remain missing. | Policy aliases, `pb_runtime_sequential_policy_dsl_smoke`, `pb_runtime_stateful_storage_policy_smoke`, and `docs/examples.md`. |
| 5-6 | Phase 5 branch output and join validation | Accepted validation evidence exists for branch output compatibility and join consumption; keep it scoped to validation, not execution. | Final candidate branch includes the validation commits plus branch-output and join-validation compile-pass/compile-fail logs. |
| 7 | Phase 5 sequential branch execution | Supported for the current sequential slice: homogeneous outputs, variant-based heterogeneous outputs plus type-list selected-output joins including duplicate alternatives by index, move-only selected-branch input consumption, explicit sequential fan-in with skipped/completed/failed slots, void-output aggregation, borrowed move-only fan-in, observer events, stateful storage, tests, and examples. | Additional backend branch execution, preemptive cancellation policy, selected-output tagged case-result policy, and broader ownership/lifetime policies need design, tests, and docs before promotion. |
| 8-9 | Descriptor/export and graph export themes | Compile-time metadata exists; DOT / JSON / text helpers (`pb::export_text` shipped) cover linear plus supported branch/fan-in pipelines with DOT label escaping, byte-equal extended goldens, and the typed `pb.core.graph.v1` schema-version contract; `pb_cli` ships `list` / `describe` / `features` for three built-in pipelines. A frozen external schema with stability promise and v1 → v2 migration policy, plus CLI/file export for arbitrary user pipelines, remain roadmap-only. | Stability promise, schema-migration policy, broader CLI surface for user pipelines, descriptor-backed compatibility fixtures, and clear separation between helper output and frozen graph export. |
| 10 | Phase 6 optional backends | Sequential backend plus thread-pool fan-in backend slice are documented/tested; external backend gates remain fail-fast. | Examples, benchmarks, broader backend branch behavior, and dependency adapter isolation before broader backend claims. |
| 11-12 | Phase 7 compiler/release requirements | GitHub matrix evidence exists for code SHA `87299c14c813753d170911239e251064cbbfee6f`: GCC C++20/C++23, Clang C++20/C++23, MSVC C++20, and clean Ubuntu package-release all passed `163/163`; normal CI also passed Clang dev, package-release, and benchmark-smoke lanes. | Keep exact-SHA compiler ID/version plus configure/build/test/package logs attached before tagging; rerun if later non-doc code changes land. |

## Runtime lane gap/evidence refresh

| Area | Moved from | Current status | Evidence | Still missing |
| --- | --- | --- | --- | --- |
| Optional backends | Missing / placeholder CMake flags only | Partial support: feature matrix, fail-fast reserved external backend flags, standalone `thread_pool`, and supported `thread_pool_backend` fan-in scheduling with deterministic ordered aggregation | `include/pb/runtime/backend.hpp`, `include/pb/runtime/thread_pool.hpp`, `include/pb/runtime/thread_pool_backend.hpp`, `pb_runtime_thread_pool_smoke`, `pb_runtime_thread_pool_fan_in_smoke`, `pb_runtime_backend_feature_matrix_smoke`, fail-fast configure checks for Taskflow/oneTBB/stdexec flags | oneTBB, Taskflow, stdexec adapters; backend examples; broader cancellation/error policy; dependency isolation target tests; backend benchmarks |
| Policy DSL | Missing full DSL | Partial: `pb::policy` / `pb::runtime::policy` exposes marker bundles and introspection for scheduling, cancellation, clone, lifetime, and existing sequential stage-storage seams. Five engine-side error-policy wrappers ship under `pb::with_*` factories (`throwing_engine`, `terminating_engine`, `ignoring_engine`, `propagating_engine`, `verbose_engine`) with **proven cross-wrapper `run()` / `try_run()` parity** across linear / branch / fan-in, **composability** (all wrappers expose `set_observer` / `get_observer` / `describe` / `descriptor`), and **`std::expected` matrix coverage**. `pb::shared_view<T>` + `pb::projected<From, Projection, Stage>` cover the runtime clone/projection policy for owned-non-copyable fan-in inputs. Full runtime-enforced `::with<pb::policy::...>` semantics over every policy axis is still missing. | `pb_policy_dsl_compile_pass`, `pb_runtime_sequential_policy_dsl_smoke`, `tests/runtime/throw_on_error_smoke.cpp`, `tests/runtime/policy_dsl_engine_smoke.cpp`, `tests/compile_pass/terminate_on_error_smoke.cpp`, `tests/runtime/policy_dsl_parity_matrix_linear.cpp`, `tests/runtime/policy_dsl_parity_matrix_branch.cpp`, `tests/runtime/policy_dsl_parity_matrix_fan_in.cpp`, `tests/runtime/policy_dsl_composability_smoke.cpp`, `tests/runtime/policy_dsl_std_expected_matrix.cpp`, `tests/runtime/fan_in_shared_view_smoke.cpp`, `tests/runtime/projected_stage_smoke.cpp`, public-header static assertions, `docs/examples.md` | Runtime-enforced `::with<pb::policy::...>` behavior for error/exception/reference-lifetime/clone/projection/contract/diagnostic verbosity/executor capability policies plus conflict diagnostics. The wrapper-layer error-policy DSL itself is parity-proven and composable. |
| Stateful stage storage | Partial / not a public guarantee | Narrow done: default-initializable sequential stages may use per-run construction or engine-stored state, including non-copyable owned members, across `run()` and `try_run()` | `pb_runtime_stateful_storage_policy_smoke`, `pb_runtime_sequential_policy_dsl_smoke`, `docs/examples.md`, `docs/roadmap-gap-map.md` | Borrowed/reference, shared/unique ownership, reset, move-in stage instances, and thread-local policies for future parallel backends |
| Standard-library runtime boundary | At risk from optional-backend placeholders | Done for this slice: runtime remains standard-library-only; external dependency backend flags fail fast, and supported backends are limited to `sequential` plus the standard-library `thread_pool_backend` fan-in slice. | Dependency grep, fail-fast configure checks for all reserved external backend flags, backend matrix marks `sequential` and `thread_pool` supported | Future dependency backend targets must keep external dependencies isolated from core/runtime public headers |

## Current release decision rule

A release note may claim the linear/sequential MVP, current adapter shapes, smoke benchmark scaffolding, package-consumer scaffolding, supported sequential branch execution, variant-based heterogeneous branch outputs including duplicate alternatives by index, move-only selected-branch input consumption, explicit fan-in joins, thread-pool backend fan-in scheduling, `pb::shared_view` / `pb::projected` clone-projection policies, the five-wrapper error-policy DSL (`throwing` / `terminating` / `ignoring` / `propagating` / `verbose` engines), the typed `pb.core.graph.v1` schema-version contract with byte-equal goldens, `pb::export_text` compact text helper, expanded `pb_cli` introspection (`list` / `describe` / `features`) over three built-in pipelines, indexed pipeline I/O aliases with named OOB diagnostics, typed `pb::features::*` C++26 feature-gate constants with regression-tested fallback, the gated `pb::reflect_stage<T>` reflection-adapter scaffold, and the GCC/Clang/MSVC/package validation matrix for exact code SHA `87299c14c813753d170911239e251064cbbfee6f`. It must keep dependency backends, preemptive cancellation, cross-wrapper `run()` / `try_run()` policy parity, runtime-enforced `::with<pb::policy::...>` DSL, frozen external descriptor/export schema, CLI/file export for arbitrary user pipelines, benchmark thresholds, stable observer ABI/event schema, working C++26 reflection adapter on a real toolchain, and CI-enforced performance budgets marked as roadmap gaps. Cross-compiler validation has **not** yet been rerun on the new working-tree SHA; rerun before tagging.

## Evidence refresh checklist

Before changing any row from roadmap/partial to shipped support, require all three:

1. Code/API evidence in the owned surface.
2. Targeted compile-pass, compile-fail, runtime, benchmark, or package tests proving the behavior.
3. Documentation/examples that state the boundary without promoting future roadmap behavior.
