

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v6.10.10 -->
<!-- Updated: 2026-05-16T05:25:07.123Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

# Code signatures

## bench

### bench/compile_time/chain_5.cpp
```
struct value
struct s1
struct s2
struct s3
struct s4
struct s5
main() → int
```

### bench/compile_time/chain_50.cpp
```
struct value
struct s1
struct s2
struct s3
struct s4
struct s5
struct s6
struct s7
struct s8
struct s9
struct s10
struct s11
struct s12
struct s13
struct s14
struct s15
struct s16
struct s17
struct s18
struct s19
struct s20
struct s21
struct s22
struct s23
struct s24
```

### bench/compile_time/include_pipeline.cpp
```
main() → int
```

### bench/runtime/sequential_5_stage.cpp
```
struct V
struct S1
struct S2
struct S3
struct S4
struct S5
main() → int
```

## examples

### examples/basic_order_pipeline.cpp
```
struct RawText
struct OrderDraft
struct ValidatedOrder
struct Receipt
struct ParseError
struct ValidationError
struct validate_order
struct parse_order
struct validate_order
struct PersistOrder
main() → int
```

### examples/error_diagnostic.cpp
```
struct RawText
struct OrderDraft
struct Receipt
struct ParseError
struct parse_order
struct PersistOrder
main() → int
```

## include

### include/pb/adapt/fn.hpp
```
struct unnamed_stage
struct name
struct fn
struct member
struct functor
struct in
struct out
struct err
struct callable_stage
struct function_stage
constexpr(named_tag<Tag>) → if
```

### include/pb/core/describe.hpp
```
struct stage_record
struct edge_record
struct pipeline_descriptor_view
struct stage_descriptor
struct edge_descriptor
struct pipeline_descriptor
```

### include/pb/core/fixed_string.hpp
```
struct fixed_string
```

### include/pb/core/meta.hpp
```
struct type_list
```

### include/pb/core/pipeline_state.hpp
```
struct pipeline
struct pipeline_state
```

### include/pb/core/stage_traits.hpp
```
struct no_error
struct error_type_or
struct stage_traits
struct stage_info
constexpr(std::convertible_to<Name, std::string_view>) → if
constexpr(requires { T::name; }) → if
constexpr(requires { T::key; }) → if
```

### include/pb/runtime/error.hpp
```
struct stage_id
struct error
```

### include/pb/runtime/observer.hpp
```
struct observer
on_stage_start(const stage_id&) → virtual void
on_stage_success(const stage_id&) → virtual void
on_stage_failure(const stage_id&, const error&) → virtual void
on_stage_exception(const stage_id&, const error&) → virtual void
```

### include/pb/runtime/result.hpp
```
class result
constexpr(streamable_error<E>) → if
requires(const E& source) → requires
constexpr(std::same_as<std::remove_cvref_t<E>, error>) → if
constexpr(is_result_v<Value>) → if
constexpr(std::same_as<Output, void>) → if
```

### include/pb/runtime/sequential.hpp
```
struct sequential
if(stage_key != "<unnamed>")
constexpr(std::same_as<ErrorType, error>) → if
notify_stage_start(observer* sink, std::size_t stage_index) → void
if(sink != nullptr)
notify_stage_success(observer* sink, std::size_t stage_index) → void
if(sink != nullptr)
notify_stage_failure(observer* sink, std::size_t stage_index, const Error& value) → void
if(sink != nullptr)
notify_stage_exception(observer* sink, std::size_t stage_index, const Error& value) → void
if(sink != nullptr)
constexpr(std::same_as<ErrorType, error>) → if
constexpr(std::constructible_from<TargetError, SourceError>) → if
constexpr(std::is_void_v<FinalOutput>) → if
constexpr(std::is_void_v<FinalOutput>) → if
constexpr(std::is_void_v<FinalOutput>) → if
```

## src

### src/runtime/error.cpp
```
switch(category)
if(stage.key == stage.name)
```

## tests

### tests/compile_fail/pipeline_stage_descriptor_non_pipeline_misuse.cpp
```
struct NotAPipeline
main() → int
```

### tests/compile_fail/pipeline_stage_error_non_pipeline_misuse.cpp
```
struct NotAPipeline
main() → int
```

### tests/runtime/sequential_adapters_smoke.cpp
```
struct Input
struct Parsed
struct MoveOnlyParsed
struct Output
struct MoveOnlyOutput
struct opaque_error
struct diagnostic_error
struct move_only_diagnostic_error
struct move_only_opaque_error
struct external_expected
struct external_void_expected
struct parse_input
struct multiply_input
struct parse_member
struct parse_functor
struct parse_functor_move_only_diagnostic
struct parse_opaque
struct parse_diagnostic
struct parse_result_move_only_error
struct parse_member_move_only_diagnostic
struct parse_member_move_only_value
struct consume_void
struct consume_void_move_only
struct direct_member_consume_void
struct direct_member_emit_move_only
```

### tests/compile_fail/adapt_direct_member_callable_mismatch.cpp
```
struct CounterName
struct Counter
bump(int) → int
```

### tests/compile_fail/adapt_direct_member_non_default_initializable.cpp
```
struct Raw
struct Parsed
struct StatefulParser
StatefulParser(int) → explicit
parse(Raw) → Parsed
```

### tests/compile_fail/adapt_direct_member_output_mismatch.cpp
```
struct Raw
struct Parsed
struct Receipt
struct Parser
parse(Raw) → Parsed
```

### tests/compile_fail/adapt_fn_callable_mismatch.cpp
```
struct Raw
struct Parsed
struct Receipt
struct parse_receipt_stage
parse_receipt(Receipt) → Parsed
```

### tests/compile_fail/adapt_fn_output_mismatch.cpp
```
struct Raw
struct Parsed
struct Receipt
parse(Raw) → Parsed
```

### tests/compile_fail/adapt_functor_callable_mismatch.cpp
```
struct Raw
struct Parsed
struct Receipt
struct Parser
```

### tests/compile_fail/adapt_functor_non_default_initializable.cpp
```
struct Raw
struct Parsed
struct StatefulParser
StatefulParser(int) → explicit
```

### tests/compile_fail/adapt_functor_output_mismatch.cpp
```
struct Raw
struct Parsed
struct Receipt
struct Parser
```

### tests/compile_fail/adapt_member_callable_mismatch.cpp
```
struct Counter
bump(int) → int
```

### tests/compile_fail/adapt_non_final_void_stage_mismatch.cpp
```
struct Input
struct Parsed
consume(Input) → void
parse(Parsed) → Parsed
```

### tests/compile_fail/duplicate_stage_edge_mismatch.cpp
```
struct Raw
struct Parsed
struct Receipt
struct Parse
main() → int
```

### tests/compile_fail/edge_descriptor_invalid_from_stage_misuse.cpp
```
struct Parsed
struct Done
struct Finish
struct NotAStage
main() → int
```

### tests/compile_fail/edge_descriptor_invalid_to_stage_misuse.cpp
```
struct Raw
struct Parsed
struct Parse
struct NotAStage
main() → int
```

### tests/compile_fail/edge_mismatch.cpp
```
struct Raw
struct Parsed
struct Receipt
struct Parse
struct Persist
```

### tests/compile_fail/invalid_stage.cpp
```
struct Raw
struct MissingOutput
```

### tests/compile_fail/missing_input_stage.cpp
```
struct Raw
struct Parsed
struct MissingInput
```

### tests/compile_fail/pipeline_describe_non_pipeline_misuse.cpp
```
struct NotAPipeline
main() → int
```

### tests/compile_fail/pipeline_descriptor_view_non_pipeline_misuse.cpp
```
struct NotAPipeline
main() → int
```

### tests/compile_fail/pipeline_edge_alias_non_pipeline_misuse.cpp
```
struct NotAPipeline
main() → int
```

### tests/compile_fail/pipeline_edge_count_non_pipeline_misuse.cpp
```
struct NotAPipeline
main() → int
```

### tests/compile_fail/pipeline_edge_from_key_non_pipeline_misuse.cpp
```
struct NotAPipeline
main() → int
```

### tests/compile_fail/pipeline_edge_from_key_zero_edge_oob.cpp
```
struct Raw
main() → int
```

### tests/compile_fail/pipeline_edge_from_name_non_pipeline_misuse.cpp
```
struct NotAPipeline
main() → int
```

### tests/compile_fail/pipeline_edge_from_stage_index_oob.cpp
```
struct Raw
struct Parsed
struct Done
struct Parse
struct Finish
main() → int
```

### tests/compile_fail/pipeline_edge_from_stage_non_pipeline_misuse.cpp
```
struct NotAPipeline
main() → int
```

### tests/compile_fail/pipeline_edge_index_oob.cpp
```
struct Raw
struct Parsed
struct Done
struct Parse
struct Finish
main() → int
```

### tests/compile_fail/pipeline_edge_to_key_non_pipeline_misuse.cpp
```
struct NotAPipeline
main() → int
```

### tests/compile_fail/pipeline_edge_to_name_non_pipeline_misuse.cpp
```
struct NotAPipeline
main() → int
```

### tests/compile_fail/pipeline_edge_to_stage_non_pipeline_misuse.cpp
```
struct NotAPipeline
main() → int
```

### tests/compile_fail/pipeline_edge_to_stage_zero_edge_oob.cpp
```
struct Raw
main() → int
```

### tests/compile_fail/pipeline_empty_non_pipeline_misuse.cpp
```
struct NotAPipeline
main() → int
```

### tests/compile_fail/pipeline_has_stage_v_misuse.cpp
```
struct Raw
struct Parse
main() → int
```

### tests/compile_fail/pipeline_input_non_pipeline_misuse.cpp
```
struct NotAPipeline
main() → int
```

### tests/compile_fail/pipeline_output_non_pipeline_misuse.cpp
```
struct NotAPipeline
main() → int
```

### tests/compile_fail/pipeline_size_non_pipeline_misuse.cpp
```
struct NotAPipeline
main() → int
```

### tests/compile_fail/pipeline_stage_alias_non_pipeline_misuse.cpp
```
struct NotAPipeline
main() → int
```

### tests/compile_fail/pipeline_stage_descriptor_zero_stage_oob.cpp
```
struct Raw
main() → int
```

### tests/compile_fail/pipeline_stage_key_index_oob.cpp
```
struct Raw
struct Parsed
struct Done
struct Parse
struct Finish
```

### tests/compile_fail/pipeline_stage_key_non_pipeline_misuse.cpp
```
struct NotAPipeline
main() → int
```

### tests/compile_fail/pipeline_stage_key_zero_stage_oob.cpp
```
struct Raw
main() → int
```

### tests/compile_fail/pipeline_stage_name_non_pipeline_misuse.cpp
```
struct NotAPipeline
main() → int
```

### tests/compile_fail/pipeline_stage_name_zero_stage_oob.cpp
```
struct Raw
main() → int
```

### tests/compile_fail/pipeline_stage_type_zero_stage_oob.cpp
```
struct Raw
main() → int
```

### tests/compile_fail/pipeline_stages_non_pipeline_misuse.cpp
```
struct NotAPipeline
main() → int
```

### tests/compile_fail/result_error_type_mismatch.cpp
```
main() → int
```

### tests/compile_fail/result_pipeline_error_type_mismatch.cpp
```
struct Input
struct Middle
struct Output
struct FirstError
struct SecondError
struct CheckedFirst
struct CheckedSecond
main() → int
```

### tests/compile_fail/sink_mismatch.cpp
```
struct Raw
struct Parsed
struct Receipt
struct Parse
```

### tests/compile_fail/stage_descriptor_invalid_stage_misuse.cpp
```
struct NotAStage
main() → int
```

### tests/compile_fail/stage_error_invalid_stage_misuse.cpp
```
struct NotAStage
main() → int
```

### tests/compile_fail/stage_input_invalid_stage_misuse.cpp
```
struct NotAStage
main() → int
```

### tests/compile_fail/stage_output_invalid_stage_misuse.cpp
```
struct NotAStage
main() → int
```

### tests/compile_fail/zero_stage_sink_mismatch.cpp
```
struct Raw
struct Receipt
main() → int
```

### tests/compile_pass/adapt_functor.cpp
```
struct Raw
struct Parsed
struct Receipt
struct parse_stage_name
struct finish_stage_name
struct Parser
struct Finisher
main() → int
```

### tests/compile_pass/core_concepts_false_paths.cpp
```
struct Raw
struct Parsed
struct Done
struct NotAStage
struct Parse
struct Finish
struct MetadataOnlyStage
main() → int
```

### tests/compile_pass/describe_pipeline.cpp
```
struct Raw
struct Parsed
struct Done
struct Parse
struct Finish
main() → int
```

### tests/compile_pass/expected_like_result_ok.cpp
```
struct Raw
struct ResultInt
main() → int
```

### tests/compile_pass/public_headers/adapt_fn.cpp
```
pb_public_header_adapt_fn() → int
```

### tests/compile_pass/public_headers/adapt_member.cpp
```
struct Counter
struct named_bump
bump(int value) → int
```

### tests/compile_pass/public_headers/adaptors.cpp
```
struct Raw
struct Parsed
struct Receipt
struct User
struct Parser
struct ReceiptSink
struct Stage
record(Receipt) → void
parse(std::string) → Raw
consume_receipt(Receipt) → void
```

### tests/compile_pass/public_headers/core_concepts.cpp
```
pb_public_header_core_concepts() → int
```

### tests/compile_pass/public_headers/core_describe.cpp
```
pb_public_header_core_describe() → int
```

### tests/compile_pass/public_headers/core_fixed_string.cpp
```
pb_public_header_core_fixed_string() → int
```

### tests/compile_pass/public_headers/core_meta.cpp
```
pb_public_header_core_meta() → int
```

### tests/compile_pass/public_headers/core_pipeline_state.cpp
```
pb_public_header_core_pipeline_state() → int
```

### tests/compile_pass/public_headers/core_stage_traits.cpp
```
pb_public_header_core_stage_traits() → int
```

### tests/compile_pass/public_headers/core_validate.cpp
```
pb_public_header_core_validate() → int
```

### tests/compile_pass/public_headers/pipeline.cpp
```
pb_public_header_pipeline() → int
```

### tests/compile_pass/public_headers/runtime_error.cpp
```
pb_public_header_runtime_error() → int
```

### tests/compile_pass/public_headers/runtime_observer.cpp
```
pb_public_header_runtime_observer() → int
```

### tests/compile_pass/public_headers/runtime_result.cpp
```
pb_public_header_runtime_result() → int
```

### tests/compile_pass/public_headers/runtime_sequential.cpp
```
pb_public_header_runtime_sequential() → int
```

### tests/compile_pass/public_headers.cpp
```
struct Raw
struct Parsed
struct Done
struct ParseError
struct Parse
struct Finish
main() → int
```

### tests/compile_pass/result_error_type_compatible.cpp
```
struct Input
struct Middle
struct Output
struct PipelineError
struct CheckedAddOne
struct CheckedDouble
if(input.value < 0)
if(input.value == 0)
main() → int
```

### tests/compile_pass/smoke_pipeline.cpp
```
struct Raw
struct Parsed
struct Normalized
struct Done
struct Parse
struct Normalize
struct Finish
main() → int
```

### tests/compile_pass/stage_info.cpp
```
struct Raw
struct Parsed
struct ParseError
struct NotAStage
struct Parse
struct StringViewName
struct ViewName
struct CStrName
struct ObjectNamed
struct ViewNamed
struct Unnamed
main() → int
```

### tests/compile_pass/stage_naming_descriptor.cpp
```
struct Raw
struct Parsed
struct Done
struct StringViewName
struct ViewName
struct CStrKey
struct Parse
struct Finish
main() → int
```

### tests/runtime/error_diagnostic_smoke.cpp
```
main() → int
```

### tests/runtime/result_smoke.cpp
```
main() → int
has_value() → bool
value() → int
error() → std::string const&
has_value() → bool
has_value() → bool
value() → void
error() → std::string const&
```

### tests/runtime/sequential_custom_error_observer_smoke.cpp
```
struct Input
struct Output
struct custom_error
struct CheckedStage
if(input.value == 0)
if(input.value < 0)
main() → int
```

### tests/runtime/sequential_exception_boundary_smoke.cpp
```
struct Input
struct Middle
struct Output
struct AddOne
struct BoundaryThrow
struct UnknownBoundaryThrow
struct ResultStart
struct ResultBoundaryThrow
struct ResultBoundaryFailure
if(input.value < 0)
if(input.value < 0)
if(input.value == 0)
main() → int
```

### tests/runtime/sequential_expected_like_smoke.cpp
```
struct Input
struct Middle
struct Output
struct external_expected
struct CheckedAddOne
struct DoubleValue
if(input.value < 0)
main() → int
```

### tests/runtime/sequential_observer_accessor_smoke.cpp
```
struct Input
struct Output
struct AddOne
main() → int
if(output.value != 21)
```

### tests/runtime/sequential_observer_smoke.cpp
```
struct Input
struct Middle
struct Output
struct AddOne
struct CheckedDouble
struct ThrowingStage
if(input.value == 0)
main() → int
```

### tests/runtime/sequential_observer_try_run_exception_smoke.cpp
```
struct Input
struct Middle
struct Output
struct AddOne
struct MaybeThrowingDouble
if(input.value < 0)
main() → int
```

### tests/runtime/sequential_result_smoke.cpp
```
struct Input
struct Middle
struct Output
struct AddOne
struct CheckedDouble
struct CheckedMayThrow
if(input.value < 0)
if(input.value == 0)
main() → int
```

### tests/runtime/sequential_result_stage_annotation_smoke.cpp
```
struct Input
struct Middle
struct Output
struct AddOne
struct CheckedDouble
if(input.value == 0)
main() → int
```

### tests/runtime/sequential_run_exception_smoke.cpp
```
struct Input
struct Middle
struct Output
struct CheckedDouble
struct UnknownThrowingFinal
if(input.value < 0)
if(input.value == 0)
main() → int
```

### tests/runtime/sequential_smoke.cpp
```
struct Input
struct Middle
struct Output
struct AddOne
struct DoubleValue
struct MoveInput
struct MoveMiddle
struct MoveOutput
struct MoveAddOne
struct MoveDoubleValue
main() → int
if(output.value != 42)
if(*move_output.value != 42)
```

### tests/runtime/sequential_try_run_custom_error_expected_like_smoke.cpp
```
struct Input
struct Output
struct custom_error
struct CheckedWithDiagnostic
if(input.value < 0)
main() → int
```

### tests/runtime/sequential_try_run_smoke.cpp
```
struct Input
struct Middle
struct Output
struct AddOne
struct MaybeThrowingDouble
struct UnknownThrowingDouble
struct CheckedDouble
struct external_expected
struct ExternallyCheckedDouble
if(input.value < 0)
if(input.value == 0)
if(input.value == 0)
main() → int
```

### tests/runtime/zero_stage_smoke.cpp
```
struct EmptyInput
struct Input
main() → int
if(output.value != 17)
if(observer.starts != 0 || observer.successes != 0 || observer.failures != 0 || observer.exceptions != 0)
```
